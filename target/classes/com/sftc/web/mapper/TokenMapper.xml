<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.1//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sftc.web.mapper.TokenMapper">
        <insert id="addToken" parameterType="com.sftc.web.model.Token">
                INSERT INTO sftc_token (create_at,create_time, is_logout, gmt_modified,
                 gmt_expiry,  user_id,access_token,refresh_token,uuid) VALUES
                 (#{create_at},#{create_time},#{is_logout},#{gmt_modified},#{gmt_expiry},#{user_id},#{access_token},#{refresh_token},#{uuid})

        </insert>
</mapper>