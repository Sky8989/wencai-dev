<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.1//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sftc.ssm.mapper.UserMapper">

    <insert id="addAddress" parameterType="Address" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO address (province, city, block, houseNumber,phone,name,addressTap,userId) VALUES
        ('11','11','11','11','11','11','11',1)

    </insert>

    <!--
    <select id="getUser" parameterType="User" resultType="User"  resultMap="getUser">
        select * from address a,users s where s.id=a.userId and s.id=1
    </select>
    <resultMap id="getUser" type="User">
        <id property="id" column="id" />
        <result property="name" column="name" />
                <collection property="addressList" ofType="Address">
                    <id property="id" column="id" />
                    <result property="name" column="name"/>
                </collection>
    </resultMap>

    -->
    <!-- 	<if test="sex!=null">
            and name='jj'
    </if> -->
    <!--
        <choose>
            <when test="sex!=null">
            sex=#{sex}
            </when>
            <when test="name!=null">
                and name=#{name}
            </when>
        </choose> -->


</mapper>